# Math SDK integration

This directory is the Neon Dynasty game folder intended to be copied into an official Stake Engine Math SDK checkout under `games/neon_dynasty/`.

The source is derived from the supplied official `0_0_cluster` reference architecture. The included `run.py` performs a local development smoke simulation without the optimizer. It does not claim certification.
