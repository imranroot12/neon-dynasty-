# Math audit tools

`fast_rtp_audit.py` uses the real Neon Dynasty GameState for high-volume statistical analysis while disabling presentation-event serialization. It is **not** a replacement for the official publication pipeline and never produces a publishable book.
